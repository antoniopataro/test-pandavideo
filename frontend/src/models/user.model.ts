export type User = {
  createdAt: string;
  email: string;
  id: string;
  name: string | null;
  updatedAt: string;
};

export type UserFromAccessToken = {
  createdAt: string;
  email: string;
  id: string;
  name: string | null;
  updatedAt: string;
};
